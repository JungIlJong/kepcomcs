<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kepco.app.domain.setting.mapper.SettingSysMapper">

    <insert id="insert" parameterType="List">
        INSERT INTO VST_SETTING (
            NAME, CONTENT
        ) VALUES
              <foreach collection="list" item="item" separator=",">
                  (#{item.key}, #{item.content})
              </foreach>
        ON DUPLICATE KEY UPDATE CONTENT = VALUES(CONTENT)
    </insert>

    <select id="selectAll" resultType="com.kepco.app.domain.setting.dto.VstSetting">
        SELECT NAME `key`, CONTENT content FROM VST_SETTING ORDER BY ID DESC
    </select>
</mapper>